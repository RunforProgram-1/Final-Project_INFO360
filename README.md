# Final-Project_INFO360
Project overview & features: User can enter their name, email, and favorite city. The input will be displayed in user list. Error handling and validation prompted when approariate.

Deployment steps: Used Render for deployment. Files stored in GitHub repository.
     Build command: npm install
     Start command: npm start

Environment variable setup: via process.env

Any special configuration (if applicable): NA
